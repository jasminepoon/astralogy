import test from 'node:test';
import assert from 'node:assert/strict';
import {BrushScene} from '../dist/brush-scene.js';
function ending(){const scene=Object.create(BrushScene.prototype),paint=[];Object.assign(scene,{w:800,h:450,zoom:1,yaw:0,pitch:0,frame:{center:[0,0],width:.8,height:.5},mode:'select',hoverLight:'old-light',data:{view:{phase:'arrived'},selected:'old-light',fieldQuote:{},tracers:[[1]],preview:{}},ctx:new Proxy({clearRect(){paint.length=0;}},{get(target,k){return target[k]??((...args)=>paint.push([k,...args]));}}),camera:{updateProjectionMatrix(){}},stars:{matrix:{set(){}},matrixWorldNeedsUpdate:false},renderer:{render(){}},scene:{},canvas:{style:{},getBoundingClientRect(){throw Error('Arrival picking must be disabled');}},handle:{hidden:false,hasPointerCapture(){return false;}},hint:{hidden:false}});return {scene,paint};}
test('verified Earth ending suppresses old selection, hover, field and preview before any overlay painting',()=>{const {scene,paint}=ending();scene.draw();assert.deepEqual(paint,[]);assert.equal(scene.hoverLight,null);assert.equal(scene.handle.hidden,true);assert.equal(scene.hint.hidden,true);scene.pointerDown({});scene.pointerMove({});scene.zoomBy(1.25);scene.setMode('select');scene.resize=()=>scene.draw();scene.resize();assert.deepEqual(paint,[]);assert.equal(scene.handle.hidden,true);});
